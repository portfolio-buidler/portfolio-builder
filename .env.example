# ---- PostgreSQL Settings ----
POSTGRES_USER=          # Database username (used to create the db user) e.g., portfolio
POSTGRES_PASSWORD=      # Database password e.g., portfolio_pw (keep in .env)
POSTGRES_DB=            # Default database name e.g., portfolio
POSTGRES_PORT=5432

# ---- Backend ----
# Port the FastAPI backend will listen on (inside your machine)
BACKEND_PORT=8000

# ---- Frontend ----
# Port the React frontend will be exposed on
FRONTEND_PORT=3000

# ---- Connection URL for backend ----
# - Inside docker-compose → host should be "db"
# - Running backend locally → host should be "localhost"
#            url format:
DATABASE_URL=postgresql+psycopg2://<user>:<password>@<host>:<port>/<dbname>


